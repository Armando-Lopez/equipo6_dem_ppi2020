{
	"version": 2,
	"builds": [
		{
			"src": "/index.js",
			"use": "@now/node-server"
		}
  ],
  "routes": [
	  { "src": "/test", "dest": "/index.js" },
	  { "src": "/test/(?<id>[^/]+)", "dest": "/index.js" }
  ]
}
