{
	"version": 2,
	"builds": [
		{
			"src": "./index.js",
			"use": "@now/node-server"
		}
  ],
  "routes": [
    { "src": "/test/(?<id>[^/]+)", "dest": "/test?id=$id" },
    { "src": "/ajustes", "dest": "/index.js" }
  ]
}
